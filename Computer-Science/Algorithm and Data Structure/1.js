// 迭代：数组求和

// function sum(array) {
//   if (!Array.isArray(array)) {
//     throw new Error('array request.')
//   }

//   let len = array.length
//   let newArray = array.pop()
//   return (len < 1) ? 0 : sum(newArray) + array[len - 1]
// }

